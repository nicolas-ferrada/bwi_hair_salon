import 'package:flutter/material.dart';

class LoginLegal extends StatelessWidget {
  const LoginLegal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('TOS and PP');
  }
}
